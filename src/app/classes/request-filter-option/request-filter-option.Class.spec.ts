import { RequestFilterOption } from './request-filter-option.Class';

describe('RequestFilterOption', () => {
  it('should create an instance', () => {
    expect(new RequestFilterOption()).toBeTruthy();
  });
});
