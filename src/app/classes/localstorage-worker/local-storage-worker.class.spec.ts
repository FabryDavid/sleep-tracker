import { LocalStorageWorker } from './local-storage-worker.class';

describe('LocalStorageWorker', () => {
  it('should create an instance', () => {
    expect(new LocalStorageWorker()).toBeTruthy();
  });
});
